import { Alfa_Slab_One, Bree_Serif } from "next/font/google";

/** Letras de la carta impresa: títulos (Alfa Slab One) y precios (Bree Serif). */
export const alfaSlab = Alfa_Slab_One({ weight: "400", subsets: ["latin"], variable: "--font-alfa-slab" });
export const bree = Bree_Serif({ weight: "400", subsets: ["latin"], variable: "--font-bree" });
